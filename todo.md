
when creating new address in a service allow for pick from any address in services or org




5. add time + not working
1. inherit schedule in service both


in the ui add a create file that runs build-template to create a new file. it can select which bucket and sub directory to put it and it can build it from an existing file and it prompts for name of the new file
