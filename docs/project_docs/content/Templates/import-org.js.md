# content/Templates/import-org.js

This script imports an org from the SF Service Guide API and generates a pre-populated HTML template file. It was used in the original file-based architecture (before MongoDB). Now largely superseded by the `/api/buckets/import-file` route which stores data in MongoDB instead of writing HTML files.

Kept as a reference for the SFSG API response mapping logic.
